<?xml version="1.0" encoding="utf-8"?>
<!-- Drop-shadow oval behind the main bubble container.
     Rendered by a plain View at elevation=0 so it never clips. -->
<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="oval">
    <solid android:color="#28000000" />
    <size
        android:width="60dp"
        android:height="60dp" />
</shape>